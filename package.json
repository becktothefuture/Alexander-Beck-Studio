{
  "name": "alexander-beck-studio-website",
  "private": true,
  "version": "1.0.0",
  "description": "Alexander Beck Studio website with interactive particle physics",
  "author": "Alexander Beck",
  "license": "MIT",
  "scripts": {
    "startup": "node scripts/dev-startup.js",
    "dev": "npm run start:source",
    "preview": "npm run build && npm start",
    "start": "cd public && python3 -m http.server 8000",
    "start:source": "cd source && python3 -m http.server 8001",
    "build": "NODE_ENV=production node build-production.js --modules",
    "build:dev": "NODE_ENV=development node build-production.js --modules",
    "watch": "chokidar 'source/**/*' 'webflow-export/**/*' -c \"npm run build:dev\"",
    "help": "echo '🚀 QUICK START:\n  npm run startup    → Interactive menu (RECOMMENDED)\n  npm run dev        → Dev mode (port 8001, instant reload)\n  npm run preview    → Production preview (port 8000)\n\n📦 BUILD:\n  npm run build      → Production build\n  npm run watch      → Auto-rebuild on changes\n\n🧪 TEST:\n  (disabled)         → Playwright removed for now\n\n💡 First time? Run: npm run startup'",
    "test": "echo 'Playwright tests are disabled for now.'",
    "clean": "rm -f source/current-config*.json",
    "figma:socket": "bunx cursor-talk-to-figma-socket",
    "figma:setup": "npm install && echo '✅ Dependencies installed. Next: Install bun (curl -fsSL https://bun.sh/install | bash)'",
    "figma:config": "node scripts/setup-figma-mcp-config.js"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.6",
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-replace": "^6.0.2",
    "@rollup/plugin-terser": "^0.4.4",
    "@tabler/icons-webfont": "^3.35.0",
    "chokidar-cli": "^3.0.0",
    "rollup": "^4.52.3",
    "terser": "^5.31.0"
  },
  "keywords": [
    "creative",
    "portfolio",
    "physics",
    "canvas",
    "animation",
    "interactive"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/alexander-beck-studio-website"
  }
}
