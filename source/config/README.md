# Config files

- `default-config.json` — runtime settings for the homepage simulation (copied to `public/config/default-config.json` + `public/js/config.json`).
- `contents-home.json` — runtime copy for UI strings (copied to `public/config/contents-home.json` + `public/js/contents-home.json`).
- `portfolio-config.json` — carousel tuning (copied to `public/config/portfolio-config.json` + `public/js/portfolio-config.json`).
- `contents-portfolio.json` — portfolio projects (copied to `public/config/contents-portfolio.json` + `public/js/contents-portfolio.json`).
