# Extracted Portfolio Sources

Each registered source gets one locator-based extraction note named `<source-id>.md`. Extraction notes preserve page, slide, URL, filename, or timecode locators and route candidate claims into project records.

