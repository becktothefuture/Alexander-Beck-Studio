# Raw Portfolio Sources

Each registered source lives in `raw/<source-id>/`. Files here are immutable evidence. Register new files with `npm run portfolio:source:add`; do not rename, edit, or replace a registered original.

