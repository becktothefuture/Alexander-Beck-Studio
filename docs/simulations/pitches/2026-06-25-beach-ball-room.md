# Beach Ball Room

- Date: 2026-06-25
- Stage: daily-rotation
- Launch path: `/lab/beach-ball-room.html`
- Daily href: `/lab/beach-ball-room.html?daily=1`

Beach Ball Room is now promoted into the daily rotation as a clear object-led simulation: a large circle-built beach ball moving through room-scale wall physics. The promoted default reduces the lab-like room-line treatment, slightly lowers the scale and rebound, and adds damping so the route reads as a finished daily piece rather than a raw controls experiment.

The constraint fit remains catalog-driven: the daily route is `/lab/beach-ball-room.html?daily=1`, preview metadata is present, and promotion is controlled by the `daily-rotation` stage in `react-app/app/src/data/simulationCatalog.json`.
